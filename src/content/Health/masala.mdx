---
title: Guide for aging South Asians
description: Collection of interesting insta reels.
navOrder: 3
---

import { videos } from './_masala/masala-data.ts';

export const grouped = videos.reduce((acc, v) => {
  (acc[v.group] ??= []).push(v);
  return acc;
}, {});

### Featured Reels

<div class="ig-reel-grid not-prose">
  {grouped["sasian-health"].map(clip => (
    <figure key={clip.id} class="ig-reel-item not-prose">
      <p class="ig-reel-title">{clip.title}</p>
      <Instagram id={clip.id} caption={false} />
    </figure>
  ))}
</div>

### Fitness

<div class="ig-reel-grid not-prose">
  {grouped["fitness"].map(clip => (
    <figure key={clip.id} class="ig-reel-item not-prose">
      <p class="ig-reel-title">{clip.title}</p>
      <Instagram id={clip.id} caption={false} />
    </figure>
  ))}
</div>